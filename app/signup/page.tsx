"use client";
import { useState } from "react";
import { supabase } from "../lib/supabase";
import { useRouter } from "next/navigation";
const ISSUES = ["High service charges","Poor building maintenance","Bad managing agents","Lack of transparency on costs","Damp / mould","Noisy neighbours","Safety concerns","Parking issues","Pest problems","Lift breakdowns"];
export default function SignUp(){const router=useRouter();const[loading,setLoading]=useState(false);const[error,setError]=useState("");const[file,setFile]=useState<File|null>(null);const[selectedIssues,setSelectedIssues]=useState<string[]>([]);const[otherChecked,setOtherChecked]=useState(false);const[otherText,setOtherText]=useState("");const[form,setForm]=useState({firstName:"",lastName:"",email:"",password:"",buildingName:"",postcode:"",flatNumber:"",totalFlats:"26-50",status:"owner",managingAgent:""});function updateForm(field:string,value:string){setForm({...form,[field]:value})}function toggleIssue(issue:string){if(selectedIssues.includes(issue)){setSelectedIssues(selectedIssues.filter(i=>i!==issue))}else{setSelectedIssues([...selectedIssues,issue])}}function getAllIssues(){const all=[...selectedIssues];if(otherChecked&&otherText.trim()){all.push(otherText.trim())}return all}function getRangeMax(range:string):number{switch(range){case"1-10":return 10;case"11-25":return 25;case"26-50":return 50;case"51-100":return 100;case"100+":return 150;default:return 50}}async function handleSubmit(){setError("");setLoading(true);try{const{data:authData,error:authError}=await supabase.auth.signUp({email:form.email,password:form.password});if(authError)throw authError;if(!authData.user)throw new Error("Sign up failed");const userId=authData.user.id;let verificationUrl="";if(file){const fileExt=file.name.split(".").pop();const filePath=`${userId}/verification.${fileExt}`;const{error:uploadError}=await supabase.storage.from("verifications").upload(filePath,file);if(uploadError)throw uploadError;verificationUrl=filePath}const{data:existingBuildings}=await supabase.from("buildings").select("id").eq("postcode",form.postcode.toUpperCase()).eq("name",form.buildingName);let buildingId:string;if(existingBuildings&&existingBuildings.length>0){buildingId=existingBuildings[0].id}else{const{data:newBuilding,error:buildingError}=await supabase.from("buildings").insert({name:form.buildingName,postcode:form.postcode.toUpperCase(),managing_agent:form.managingAgent,total_flats:getRangeMax(form.totalFlats)}).select("id").single();if(buildingError)throw buildingError;buildingId=newBuilding.id}const allIssues=getAllIssues();const{error:profileError}=await supabase.from("profiles").insert({id:userId,first_name:form.firstName,last_name:form.lastName,email:form.email,building_id:buildingId,flat_number:form.flatNumber,status:form.status,top_issue:allIssues.join(", "),verification_url:verificationUrl});if(profileError)throw profileError;for(const issue of allIssues){await supabase.from("issues").insert({building_id:buildingId,profile_id:userId,title:issue})}router.push("/dashboard")}catch(err:unknown){if(err instanceof Error){setError(err.message)}else{setError("Something went wrong")}}finally{setLoading(false)}}return(<div className="min-h-screen bg-gray-950 text-white py-12 px-4"><div className="max-w-md mx-auto"><h1 className="text-3xl font-bold text-orange-400 mb-2">Join BlockVoice</h1><p className="text-gray-400 mb-8">Sign up to connect with your building.</p>{error&&(<div className="bg-red-900 text-red-200 p-3 rounded-lg mb-4">{error}</div>)}<div className="space-y-4"><div className="grid grid-cols-2 gap-4"><div><label className="block text-sm text-gray-400 mb-1">First Name</label><input type="text" value={form.firstName} onChange={e=>updateForm("firstName",e.target.value)} className="w-full bg-gray-800 rounded-lg px-4 py-2 text-white"/></div><div><label className="block text-sm text-gray-400 mb-1">Last Name</label><input type="text" value={form.lastName} onChange={e=>updateForm("lastName",e.target.value)} className="w-full bg-gray-800 rounded-lg px-4 py-2 text-white"/></div></div><div><label className="block text-sm text-gray-400 mb-1">Email</label><input type="email" value={form.email} onChange={e=>updateForm("email",e.target.value)} className="w-full bg-gray-800 rounded-lg px-4 py-2 text-white"/></div><div><label className="block text-sm text-gray-400 mb-1">Password</label><input type="password" value={form.password} onChange={e=>updateForm("password",e.target.value)} className="w-full bg-gray-800 rounded-lg px-4 py-2 text-white"/></div><div><label className="block text-sm text-gray-400 mb-1">Building Name</label><input type="text" value={form.buildingName} onChange={e=>updateForm("buildingName",e.target.value)} placeholder="e.g. Maple Court" className="w-full bg-gray-800 rounded-lg px-4 py-2 text-white"/></div><div className="grid grid-cols-2 gap-4"><div><label className="block text-sm text-gray-400 mb-1">Postcode</label><input type="text" value={form.postcode} onChange={e=>updateForm("postcode",e.target.value)} placeholder="e.g. SW1A 1AA" className="w-full bg-gray-800 rounded-lg px-4 py-2 text-white"/></div><div><label className="block text-sm text-gray-400 mb-1">Flat Number</label><input type="text" value={form.flatNumber} onChange={e=>updateForm("flatNumber",e.target.value)} placeholder="e.g. 12" className="w-full bg-gray-800 rounded-lg px-4 py-2 text-white"/></div></div><div><label className="block text-sm text-gray-400 mb-1">How many flats in your building?</label><select value={form.totalFlats} onChange={e=>updateForm("totalFlats",e.target.value)} className="w-full bg-gray-800 rounded-lg px-4 py-2 text-white"><option value="1-10">1 - 10 flats</option><option value="11-25">11 - 25 flats</option><option value="26-50">26 - 50 flats</option><option value="51-100">51 - 100 flats</option><option value="100+">100+ flats</option></select></div><div><label className="block text-sm text-gray-400 mb-1">Are you an owner or tenant?</label><select value={form.status} onChange={e=>updateForm("status",e.target.value)} className="w-full bg-gray-800 rounded-lg px-4 py-2 text-white"><option value="owner">Owner</option><option value="tenant">Tenant</option></select></div><div><label className="block text-sm text-gray-400 mb-1">Managing Agent Name</label><input type="text" value={form.managingAgent} onChange={e=>updateForm("managingAgent",e.target.value)} placeholder="e.g. FirstPort" className="w-full bg-gray-800 rounded-lg px-4 py-2 text-white"/></div><div><label className="block text-sm text-gray-400 mb-2">Your Building Issues (select all that apply)</label><div className="space-y-1">{ISSUES.map(issue=>(<label key={issue} className="flex items-center gap-2 bg-gray-800 rounded-lg px-3 py-1.5 cursor-pointer hover:bg-gray-700 text-sm"><input type="checkbox" checked={selectedIssues.includes(issue)} onChange={()=>toggleIssue(issue)} className="w-3.5 h-3.5 accent-orange-500"/><span>{issue}</span></label>))}<label className="flex items-center gap-2 bg-gray-800 rounded-lg px-3 py-1.5 cursor-pointer hover:bg-gray-700 text-sm"><input type="checkbox" checked={otherChecked} onChange={()=>setOtherChecked(!otherChecked)} className="w-3.5 h-3.5 accent-orange-500"/><span>Other</span></label>{otherChecked&&(<input type="text" value={otherText} onChange={e=>setOtherText(e.target.value)} placeholder="Describe your issue..." className="w-full bg-gray-700 rounded-lg px-3 py-1.5 text-white text-sm mt-1"/>)}</div></div><div><label className="block text-sm text-gray-400 mb-1">Upload Verification</label><p className="text-xs text-gray-500 mb-2">We need a photo of a recent utility bill or council tax letter to verify you live at this address. This keeps your building group safe from outsiders.</p><input type="file" accept="image/*,.pdf" onChange={e=>{if(e.target.files)setFile(e.target.files[0])}} className="w-full bg-gray-800 rounded-lg px-4 py-2 text-white"/></div><button onClick={handleSubmit} disabled={loading} className="w-full bg-orange-500 hover:bg-orange-600 text-white py-3 rounded-lg font-semibold text-lg disabled:opacity-50">{loading?"Creating account...":"Sign Up"}</button></div></div></div>)}
